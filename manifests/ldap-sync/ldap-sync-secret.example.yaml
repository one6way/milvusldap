# Copy to ldap-sync-secret.yaml, fill values, do NOT commit real secrets.
apiVersion: v1
kind: Secret
metadata:
  name: milvus-ldap-sync
  namespace: milvus
type: Opaque
stringData:
  LDAP_BIND_PASSWORD: "{{ LDAP_SVC_PASSWORD }}"
  MILVUS_ROOT_PASSWORD: "user"
  MILVUS_SYNC_DEFAULT_PASSWORD: "ChangeMeSync1"
