# Copy to ldap-auth-secret.yaml — do NOT commit real secrets.
apiVersion: v1
kind: Secret
metadata:
  name: ldap-auth-extauthz
  namespace: milvus
type: Opaque
stringData:
  LDAP_BIND_PASSWORD: "{{ LDAP_SVC_PASSWORD }}"
  MILVUS_SYNC_DEFAULT_PASSWORD: "**********"
