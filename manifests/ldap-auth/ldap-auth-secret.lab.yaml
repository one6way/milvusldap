# Lab secret — same passwords as ldap-sync lab.
apiVersion: v1
kind: Secret
metadata:
  name: ldap-auth-extauthz
  namespace: milvus
type: Opaque
stringData:
  LDAP_BIND_PASSWORD: "**********"
  MILVUS_SYNC_DEFAULT_PASSWORD: "**********"
