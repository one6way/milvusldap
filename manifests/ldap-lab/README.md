# OpenLDAP lab (kind only)

Пароли стенда (не для prod): **`**********`**

- LDAP admin: `cn=admin,dc=lab,dc=local`
- Пользователи: `testuser`, `milvus655` — LDAP-пароль и Milvus sync-пароль: `**********`
- Milvus root (kind profile): `root` / `**********` — см. `values/values-kind-localpath.yaml`

После клонирования репо для lab ничего менять не нужно: все `*.lab.yaml` и seed LDIF уже согласованы.
