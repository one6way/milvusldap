# Тесты: локальный venv
tests/.venv/
**/__pycache__/
*.pyc

# Локальные артефакты prep (tar образов — не в Git; см. docker/ и images/*/Dockerfile)
artifacts/
*.tar.gz
*.tar

# Локальные экспорты образов (только Dockerfile в Git)
images/export/
images/ispravleno/
images/init-base-nonroot/wheels/
docker/milvus-ldap-sync/wheels-alpine/
docker/ldap-auth-extauthz/wheels-alpine/
artifacts/ldap-alpine-wheels.tar.gz

# Локальные бандлы всего стека
milvus-delivery-bundle*.tar.gz

# IDE / macOS
.idea/
.vscode/
.cursor/
.DS_Store

# Временные логи демо (если кладут в репо)
*.log
